# themoviedb.org 계정 설정 → API 에서 무료 발급 (결제정보 불필요)
TMDB_API_KEY=

# 계측. 비워두면 스크립트를 아예 안 받고 아무것도 안 보낸다.
# GA4 속성의 측정 ID (G-XXXXXXXXXX). 빌드 때 값이 코드에 박히므로,
# 배포에 넣으려면 deploy 전에 이 파일이나 환경에 있어야 한다.
NEXT_PUBLIC_GA_ID=
