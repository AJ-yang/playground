/**
 * 손잡이 전부. **여기가 단일 진실 공급원이다** — 수치는 코드 곳곳이 아니라
 * 이 파일에만 있다.
 *
 * GDD 9장이 "열린 문제"로 남긴 것들의 현재 값이 여기 있다. 값 자체보다
 * **왜 이 값인가**가 중요하므로, 근거가 있는 것에는 근거를 적어 둔다. 근거가
 * 없는 것은 "아직 근거 없음"이라고 적는다 — 그래야 나중에 무엇을 먼저
 * 만져봐야 하는지 알 수 있다.
 */

/**
 * 기준 길이 — **지역 한 변**(월드 단위). 나머지 거리는 전부 이 값에 상대적이다.
 *
 * 예전에는 "칸 한 변"이었고 34였다. 맵이 다리로 이어진 아홉 섬에서 하나로
 * 이어진 300×180 땅으로 바뀌면서(`data/land.ts`) 지역이 60으로 커졌고,
 * 거리 수치가 전부 같이 커져야 했다 — 시야 14로는 300짜리 맵에서 장님이 된다.
 */
import { REGION as TILE } from './land'
export { REGION as TILE } from './land'

export const TUNING = {
  /**
   * 지휘 반경 (GDD 3.1, 9장).
   *
   * **지름이 지역 한 변**이 되게 잡았다(반지름 30).
   *
   * 섬이던 시절의 제약은 "한 칸보다 크고 두 칸보다 작다"였다. 땅이 이어진
   * 지금은 전선이 지역 경계에 안 묶이므로 그 제약이 그대로는 안 통한다.
   * 대신 같은 뜻을 절대 크기로 옮겼다 — **한 번에 한 전선**을 덮는 크기여야
   * 하고, 유닛 열 남짓이 붙어 싸우는 자리가 대략 그만하다.
   */
  commandRadius: TILE * 0.5,

  /**
   * 강림해 있는 동안 신이 걷는 속도.
   *
   * 짝이 되던 `avatarSpeedCommanded`는 사라졌다. 신은 이제 부감에서 판 위를
   * 걷지 않으므로 비교할 상대가 없다 — 강림의 유인은 속도가 아니라 **반경
   * 그 자체**가 되었다(GDD 3.2).
   */
  avatarSpeedDriven: 23.1,

  /**
   * 강림 재사용 대기 (초).
   *
   * 강림이 걸어가는 것이 아니라 내려꽂히는 것이 되면서 필요해진 값이다.
   * 없으면 내려갔다 즉시 올라와 맵 반대편에 다시 내려갈 수 있고, 그러면
   * 반경을 공짜로 순간이동시키는 것이라 "여기가 결전지다"라는 결정이
   * 통째로 사라진다.
   *
   * 8초는 첫 값이다 — 한 번 붙은 싸움이 끝날 때까지는 못 옮기고, 다음
   * 싸움에는 늦지 않을 정도. 사람이 해봐야 아는 숫자다(GDD 9장).
   */
  descendCooldown: 8,

  /** 1인칭 시선 감도(라디안 / 픽셀). */
  lookSensitivity: 0.0022,

  /**
   * 점령 (GDD 4.3).
   *
   * 타일 위에 내 유닛만 있으면 점령이 차오른다. 적이나 중립이 함께 있으면
   * 멈춘다 — 점령은 **싸움이 끝난 뒤에** 일어나는 일이다.
   */
  captureSeconds: 6,
  /** 점령이 풀리는 속도는 차오르는 속도보다 느리다. 먹은 땅이 쉽게 뒤집히면
   *  영역이라는 개념이 안 생긴다. */
  decaySeconds: 14,

  /**
   * 경제.
   *
   * 자원은 하나(은/silver)뿐이다. 종류를 늘리면 배울 것이 늘어나는데, v1이
   * 알아내려는 것은 경제가 아니라 강림이다(GDD 6.1).
   */
  startingSilver: 120,
  silverPerTilePerSecond: 1.15,
  /** 본진이 그냥 주는 기본 수입. 초반에 아무것도 못 하는 시간을 없앤다. */
  silverBasePerSecond: 2.2,

  /** 생산 큐는 한 줄이다. 동시 생산은 손이 많이 가는 만큼 이득이 되어
   *  강림에서 손을 떼게 만든다. */
  maxQueue: 5,

  /** 인구 상한. 유닛이 너무 많아지면 지휘 반경이 무의미해진다 —
   *  반경 밖 부대만으로도 이길 수 있게 되기 때문이다. 일꾼은 여기서 뺀다. */
  maxUnits: 14,

  /**
   * 일꾼 상한 (GDD 4.6).
   *
   * 병력 상한과 **따로 센다.** 같은 칸을 두고 다투게 하면 "일꾼 하나에 병사
   * 하나"라는 뻣뻣한 교환만 남는데, 진짜 결정은 그게 아니라 **지금 은을 경제에
   * 넣을 것인가 군대에 넣을 것인가**다. 초반에 일꾼을 뽑으면 지금 약해지고
   * 나중에 강해진다 — 그 시점 선택이 결정이다.
   */
  maxWorkers: 6,

  /**
   * 칸 하나가 받을 수 있는 일꾼 수.
   *
   * 이 숫자가 경제를 강림에 묶는 장치다. 정원이 차면 더 벌 방법이 **땅을 더
   * 먹는 것**뿐이고, 앞으로 나가는 가장 빠른 길은 강림이다(GDD 3.2).
   */
  workersPerTile: 2,

  /** 일꾼 하나가 자기 칸에 더해 주는 초당 은. 회수까지 25초쯤 걸린다. */
  silverPerWorker: 1.2,

  /** 본진 체력. 20분이 아니라 5~8분 판이므로 짧게 잡았다(GDD 6.3). */
  keepHp: 900,

  /**
   * 안개 (GDD 4.1).
   *
   * 칸의 **땅 사각형까지의 거리**로 재므로, 이웃 칸의 가장 가까운 모서리는
   * 칸 중심에서 `TILE - TILE_LAND/2 = 20`만큼 떨어져 있다. 유닛 시야를 그보다
   * 작게 잡아야 "칸 한가운데 서 있으면 옆 칸이 다 보인다"가 되지 않는다 —
   * 이웃을 보려면 다리 쪽으로 나가야 하고, 그 걸음이 곧 탐험이다.
   */
  visionUnit: TILE * 0.55,
  visionKeep: TILE * 0.8,
  visionAvatar: TILE * 0.72,
  /**
   * 전초의 시야는 이웃과 대각선까지 덮는다(대각선 모서리까지 약 28).
   * 무너진 성채를 먹을 이유가 통째로 이 숫자 하나다 — 즉효가 없는 대신
   * 판을 보는 눈이 생긴다(GDD 4.3).
   */
  visionOutpost: TILE * 1.45,

  /** 승리 판정 뒤 한 박자 쉬고 배너를 띄운다. */
  endBannerDelay: 1.2,
} as const

/**
 * 방패벽 보너스 (GDD 6.2).
 *
 * 지휘 반경 안에 있는 유닛이 받는 배수. **이것이 지휘 반경을 눈에 보이게
 * 만드는 장치다** — 반경이 지나갈 때 유닛이 실제로 단단해지는 것을 보아야
 * 플레이어가 규칙을 설명 없이 알아챈다(GDD 6.2가 바이킹을 고른 이유).
 */
export const COMMANDED_BONUS = {
  /** 방패병은 방어가, 도끼병은 공격이 오른다. 유닛마다 다른 이유는
   *  "누구를 반경에 넣을 것인가"까지 결정이 되게 하기 위해서다. */
  shieldDefense: 1.85,
  axeAttack: 1.6,
  /** 반경 안에서는 이동도 조금 빠르다. 대열이 흐트러지지 않는다는 뜻. */
  speed: 1.15,
} as const
