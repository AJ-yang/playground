# 족장 (chieftain)

**내가 어디 내려서느냐가 전투력이다.**

부감으로 지휘하는 RTS인데, 나는 판 위에 없다. **밖에서 내려다보는 신**이고,
`Tab`으로 내려갈 자리를 짚으면 거기 번개가 꽂히고 몸을 얻는다. 그 순간 그
자리에 지휘 반경이 켜져서 둘레의 부대가 더 빨리 걷고 더 세게 친다.

반경은 **내려온 자리에만** 생긴다. 그래서 두 전선을 동시에 강하게 만들 수 없고,
어디에 내려설 것인가가 이 게임의 유일한 큰 결정이 된다. 신은 죽지 않는다 —
대가는 죽음이 아니라, 내려가 있는 동안 판 전체를 보는 눈을 잃는다는 것이다.

**놀아 보기** → <https://aj-yang.github.io/playground/chieftain/>

```bash
npm install
npm run dev        # http://localhost:5174
npm run build      # tsc --noEmit 먼저 돌고 dist를 굽는다
```

## 조작

| | 부감 | 1인칭 (강림 중) |
| --- | --- | --- |
| 좌클릭 | 유닛을 고른다 (**끌면 여럿**) | 보는 쪽으로 부대를 |
| 우클릭 | 고른 부대를 그리로 (적을 짚으면 공격) | — |
| WASD | 화면을 민다 | 직접 걷는다 |
| 커서를 화면 끝에 | 그쪽으로 화면이 밀린다 | — |
| 1 · 2 · 4 | 방패병 · 도끼병 · 일꾼 생산 | 같음 |
| 3 | — | 여기에 전진 기지 |
| Tab | **강림 조준** → 짚으면 내려간다 | 올라간다 |
| Esc | 선택 해제 / 조준 취소 | 올라간다 |

고른 부대의 상태는 화면 아래에 뜬다. 미니맵(우하단)도 3D 화면과 **같은 두
버튼**을 쓴다 — 좌클릭은 화면 이동(조준 중이면 강림), 우클릭은 부대 이동.

가장자리 스크롤은 끝에 가까울수록 빨라진다. 미니맵 위에서는 안 밀리고(구석에
붙어 있어서 겹친다), 커서가 창을 벗어나면 멈춘다.

**반경이 강림해야만 존재한다는 것이 강림의 유인**이다. 안 내려가면 내 부대는
그냥 평범한 부대이고, 내려가면 그 자리가 강해진다. 대신 내려가 있는 동안은
눈앞밖에 못 보고, 올라온 뒤 몇 초간은 다시 못 내려간다.

## 지금 상태

**v1 수직 슬라이스.** 노르드 미러 매치 × 피오르드 해안, 상대는 컴퓨터 하나.
강림하면 **토르**가 된다 — 망치와 번개가 "지금 여기에 신이 내렸다"를 글자 없이
말한다. 미러 매치라 양쪽 다 토르이고 색만 다르다.

땅은 300×180짜리 **한 덩어리**다. 점령·안개·수입은 그 위에 얹은 5×3 지역이
단위이고, 걷는 것은 2.5짜리 통행 격자 위에서 길을 찾는다. 목은 다리가 아니라
파고든 만에서 나온다 — 왜 섬 아홉 개를 버렸는지는 GDD 4.1에 적혀 있다.

검증할 것이 강림 하나뿐이라 나머지는 일부러 안 만들었다 — PvP도, 절차 생성도,
랜덤 이벤트도, 다른 종족·맵도 없다. 이유는 [`docs/GDD.md`](docs/GDD.md) 6장에
적혀 있다.

**합격 기준은 재미있다는 말이 아니라 행동이다.** 규칙 모르는 사람 3명에게 설명 없이
쥐어주고 화면만 본다. 한 번도 강림하지 않거나, 강림 후 끝까지 안 올라오면
불합격이다(GDD 6.5). 판이 끝나면 강림 횟수와 1인칭 체류 비율이 배너에 뜬다 —
나중에 부인할 수 없게 하려고 화면에 남긴다.

## 코드가 어디 있나

| 무엇 | 어디 |
| --- | --- |
| **손잡이 전부** (지휘 반경·아바타 속도·시야·경제) | [`src/data/tuning.ts`](src/data/tuning.ts) |
| 핵심 3규칙 — 지휘 반경·강림·무적 아바타 | [`src/game/Game.ts`](src/game/Game.ts) |
| 땅 — 지역·점령·안개·길찾기 | [`src/game/Board.ts`](src/game/Board.ts) |
| 유닛 고르기 (클릭·드래그) | [`src/ui/select.ts`](src/ui/select.ts) |
| 이어진 맵·해안선·거울상 대칭 | [`src/data/land.ts`](src/data/land.ts) |
| 미니맵 | [`src/ui/Minimap.ts`](src/ui/Minimap.ts) |
| 결정론 수학 (sin·cos·atan2를 직접 짠 이유) | [`src/core/det.ts`](src/core/det.ts) |
| 중립 세 갈래 | [`src/data/neutrals.ts`](src/data/neutrals.ts) |
| 컴퓨터 상대 | [`src/game/Ai.ts`](src/game/Ai.ts) |

수치는 `src/data/` 아래가 단일 진실 공급원이다. `Game`은 **렌더링을 전혀
모른다** — 나중에 결정론적 락스텝으로 PvP를 얹을 때 이 클래스만 두 클라이언트에서
똑같이 돌면 되게 하려는 것이다(GDD 7.2).

## 다음에 만져야 할 숫자

GDD 9장이 "열린 문제"로 남긴 것들이고, 그중 하나가 다른 것들보다 훨씬 중요하다:

**`descendCooldown`** (지금 8초). 강림이 걸어가는 것이 아니라 내려꽂히는 것이
된 뒤로, 이것이 "얼마나 자주 반경을 옮길 수 있는가"를 혼자 정한다. 짧으면
반경이 맵을 날아다니고 "여기가 결전지다"라는 결정이 사라지며, 길면 한 번 잘못
내려간 것이 판을 결정한다.
